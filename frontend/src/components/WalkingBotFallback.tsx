import MaxeMark from './Maxe/MaxeMark';

/** Temporary articulated vector. This is not the missing ten-frame character sheet. */
export default function WalkingBotFallback() {
  return <span className="walking-bot__fallback"><MaxeMark profile /></span>;
}
