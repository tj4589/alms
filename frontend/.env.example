VITE_API_BASE_URL=http://127.0.0.1:8001

# Development-only UI bypass. Vite exposes VITE_* values to the browser.
# This is ignored by production builds and is not a security boundary.
VITE_DEV_AUTH_PASS=exammind-dev
